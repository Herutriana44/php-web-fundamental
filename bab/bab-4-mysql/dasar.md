# Bab 4: MySQL Basic

MySQL adalah sistem manajemen database relasional (RDBMS) yang populer.

## Perintah Dasar
- `CREATE DATABASE nama_db;`
- `CREATE TABLE nama_tabel (kolom1 tipe_data, ...);`
- `INSERT INTO nama_tabel VALUES (nilai1, ...);`
- `SELECT * FROM nama_tabel;`
